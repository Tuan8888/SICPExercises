;; Exercise 1.39
;; 近似计算tan
#lang racket
(require "cont-frac-iterative.rkt")

(define (tan-cf x k)
  (cont-frac-iterative (lambda(y) (- (* y 2) 1))
                       (lambda(y) (- x))
                       k))

(tan-cf 1 30)

